
        # box = QComboBox()
        box = QLineEdit()
        completer = QCompleter(["aaaa", "aaa", "b", "c"])
        completer.setCaseSensitivity(Qt.CaseInsensitive)
        box.setCompleter(completer)
        # box.setDuplicatesEnabled(False)
        # box.setEditable(True)
        # box.addItems(sorted(self.sample_materials))
        box.setText(color)
        self.table.setCellWidget(nRows, 2, box)
